# module_lint

`module_lint` is a placeholder workspace for future module validation tooling.

Planned checks:

- manifest schema validation against `data/schemas/module.schema.json`
- duplicate module id detection
- category/path consistency checks
- required option metadata checks
- safety level policy checks

Current status:

- scaffold only
- no executable linter implemented yet
