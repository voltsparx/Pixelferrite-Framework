# report_export

`report_export` is a placeholder workspace for future report export utilities.

Planned capabilities:

- convert report text into structured `json`/`jsonl`
- export templates for SIEM-compatible formats
- merge multi-workspace report bundles
- hash/sign report outputs for integrity tracking

Current status:

- scaffold only
- no executable export utility implemented yet
